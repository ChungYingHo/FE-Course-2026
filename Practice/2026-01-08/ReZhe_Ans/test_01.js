// Write a JavaScript program that:
// 1. Uses `prompt()` to ask the user for two numbers.
// 2. Adds the two numbers together.
// 3. Uses `alert()` to display the result.

const firstNumber = Number(prompt('請輸入第一個數字'))
const secondNumber = Number(prompt('請輸入第二個數字'))

alert('兩個數字的總和' + (firstNumber + secondNumber))
